# pytest.ini
[pytest]
pythonpath = document-processing/src
testpaths = document-processing/tests
python_files = test_*.py
asyncio_mode = auto
markers =
    asyncio: mark test as async
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
; ...existing code...