# pytest.ini
[pytest]
pythonpath = document_processing
testpaths = document_processing/tests
python_files = test_*.py
asyncio_mode = auto
markers =
    asyncio: mark test as async
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
; ...existing code...